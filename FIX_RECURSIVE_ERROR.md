# 修复 `vercel dev` 递归调用错误 - 完整指南

## 问题说明

**`package.json` 中的 `"dev": "vercel dev"` 是正常的，不是问题！**

这个配置只是定义了一个 npm 脚本，让你可以通过 `npm run dev` 来运行 `vercel dev`。

## 真正的问题

真正的问题在于 **Vercel Dashboard 中的项目设置**：

**Settings → General → Framework Settings → Development Command**

如果这个字段被设置为 `vercel dev` 或 `npm run dev`，就会导致递归调用。

## 发现的问题

你的账户中有两个项目：
- `real-focus` (项目 ID: `prj_jRDnYI2Wcufesy2BMlVpHeqqDVMQ`)
- `real-focus` (已部署到 https://real-focus-two.vercel.app)

当前本地链接的是 `real-focus` 项目。

## 修复步骤

### 步骤 1：确认当前链接的项目

运行：
```bash
cat .vercel/project.json
```

查看 `projectName` 字段，确认是 `real-focus`。

### 步骤 2：在 Dashboard 中修复

1. 访问 [Vercel Dashboard](https://vercel.com/dashboard)
2. **找到对应的项目**（`real-focus`）
3. 进入 **Settings → General → Framework Settings**
4. 找到 **Development Command** 字段
5. **确保该字段完全为空**（不是显示 "None"，而是输入框完全空白）
6. 如果旁边有 **Override** 开关，确保它是**关闭**状态（灰色）
7. 点击页面底部的 **Save** 按钮

### 步骤 3：验证修复

修复后，运行：

```bash
vercel dev
```

应该看到：

```
Vercel CLI 48.8.2
...
Ready! Available at http://localhost:3000
```

而不是递归调用错误。

## 为什么 Dashboard 显示 "None" 仍然有问题？

可能的原因：
1. **Override 开关是开启的**：即使显示 "None"，如果 Override 开关是开启的，Vercel 仍然会使用这个设置
2. **缓存问题**：本地 `.vercel` 目录可能有旧配置
3. **多个项目混淆**：可能修复了错误项目的设置

## 如果仍然无法修复

### 方案 A：重新链接到正确的项目

```bash
# 删除本地链接
rm -rf .vercel

# 重新链接，选择正确的项目
vercel link
```

### 方案 B：检查两个项目的设置

确保 **项目**（`real-focus`）的 Development Command 是空的。

## 总结

- ✅ `package.json` 中的 `"dev": "vercel dev"` 是正常的
- ❌ 问题在 Vercel Dashboard 的项目设置中
- 🔧 需要在 Dashboard 中清空 "Development Command" 字段，并确保 Override 开关是关闭的

